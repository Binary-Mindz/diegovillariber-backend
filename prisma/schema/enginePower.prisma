model EnginePower {
  id                String          @id @default(uuid()) @db.Uuid
  advancedCarDataId String          @unique @db.Uuid
  advancedCarData   AdvancedCarData @relation(fields: [advancedCarDataId], references: [id])

  tires  String?
  wheels String?
}
